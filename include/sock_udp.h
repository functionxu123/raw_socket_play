#ifndef SOCK_UDP_H
#define SOCK_UDP_H

#include <sock_trans.h>


class sock_udp : public sock_trans
{
    public:
        sock_udp();
        virtual ~sock_udp();

    protected:

    private:
};

#endif // SOCK_UDP_H
