#ifndef SOCK_TCP_H
#define SOCK_TCP_H

#include <sock_trans.h>


class sock_tcp : public sock_trans
{
    public:
        sock_tcp();
        virtual ~sock_tcp();

    protected:

    private:
};

#endif // SOCK_TCP_H
